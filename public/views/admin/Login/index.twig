{% extends layout %}

{% block content %}
  <div class="d-flex justify-content-center align-items-center flex-column content">
    <div class="right-panel"></div>
    <h4 class="text-center custom__title">Graff CMS</h4>
    <form class="my__form" data-action="login/check">
      <label>
        <i class="fa fa-user"></i>
        <input name="name" type="text" placeholder="Podaj login" value="{{ name }}">
      </label>
      <label>
        <i class="fa fa-lock"></i>
        <input name="password" type="password" placeholder="Podaj hasło" value="{{ password }}">
      </label>
      {{ csrf() }}
      <input type="submit" value="Zaloguj">
    </form>
  </div>
{% endblock %}
